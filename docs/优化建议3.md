🛠️ 核心改进建议总结
1. 全域语言汉化 (Localization)
界面描述：所有描述性文字（如 "Total Balance", "System Health", "Scoring Weights" 等）全部替换为中文。

词汇对齐：

Dashboard → 仪表盘

Signals → 信号雷达

Settings → 系统设置

System Health → 系统健康度（同时修正截图中的拼写错误 "Heath"）

2. 信号展示模块重构 (Signals Table)
为了满足你对各项指标详细体现的需求，建议将目前的卡片流改为高信息密度的专业数据表格。

表头维度：增加 打分、价格、方向、影线占比、EMA 距离、ATR 波动率 等详细指标。

交互增强：

排序：支持点击表头对“时间”和“分数”进行升/降序排列。

过滤：在表格上方设置快捷筛选器，支持按“币种”和“多空”快速定位信号。

视觉分级：分数列依然保留颜色分级逻辑（金色/蓝色/灰色），让你在快速滚动列表时能瞬间捕捉高质量信号。

3. 系统设置模块优化 (Settings)
多选配置：将“监控币种”从输入框改为多选下拉框，预设选项为：BTCUSDT, ETHUSDT, SOLUSDT, BNBUSDT。

权重联动：三个滑动条（形态、趋势、波动）应实现逻辑关联，确保总和始终等于 100%。

精度修正：前端强制将风控参数（如回撤距离）限制为 2 位小数，消除 3.5000000000000004 这种浮点数显示异常。

4. 仪表盘性能指标完善 (Dashboard)
状态同步：修正截图中的“数据存在但状态显示 Offline”的矛盾。

频率监控：在“API 权重消耗”进度条旁增加“重置倒计时”，这对于 24/7 不间断运行的稳定性监控至关重要。